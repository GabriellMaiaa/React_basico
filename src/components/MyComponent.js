const MyComponent = () => {
    // Esse componente vai ser um teste para mostrar
    //como fica qaundo um componente é exportado para dentro de outro.
    return (
        <div>
            <h2>Teste par aver como vai ser a importação para outro componente</h2>
        </div>
    );
};

export default MyComponent;